# 🔹 Comparison Table

|Feature|**Snapshot**|**AMI**|**Launch Template**|**Launch Configuration**|
|---|---|---|---|---|
|**What is it?**|Backup of an **EBS volume** (disk)|Blueprint of a **server (OS + software + config)**|Blueprint of **how to launch EC2** (instance type, key pair, networking, etc.)|Older version of launch template (now legacy)|
|**Contains**|Only storage data (disk blocks)|Snapshot of root volume + launch permissions + block mapping|Instance type, AMI ID, key pair, security group, IAM role, networking, storage options, tags|Similar to launch template but less flexible|
|**Used For**|Backup, restore, migration, resizing EBS|Launching EC2 instances with identical setup|Automating EC2 launches (especially in Auto Scaling Groups)|Same as template but limited|
|**Can it launch EC2 directly?**|❌ No|✅ Yes|✅ Yes|✅ Yes|
|**Relation**|Building block for AMI|Uses snapshots internally|Refers to AMI ID + other configs|Refers to AMI ID + other configs|
|**Flexibility**|Just disk backup|Full server image|Very flexible (supports multiple versions, parameters, spot instances)|Less flexible (deprecated, replaced by Launch Templates)|


✅ **Final Memory Shortcut**:

- **Snapshot = Data backup**
    
- **AMI = Machine image**
    
- **Launch Template = EC2 launch settings (modern)**
    
- **Launch Configuration = Same thing, old version**

