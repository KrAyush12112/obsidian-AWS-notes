If you want to allow multiple trusted entity type (AWS services, AWS account, Web identity, SAML 2.0 Federation) in on place then select Custon trust policy.

example - One sevices commpuncate with another. also this service can be use by user within account or another account.

**`Condition`**: This is where the "custom" aspect comes in. It allows you to add specific requirements that must be met for the `AssumeRole` action to be allowed.