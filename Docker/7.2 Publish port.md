## 🔹 Definition: Published Ports

- By default, container ke andar chal raha service **sirf container ke internal IP** pe accessible hota hai (jaise `172.17.x.x`).
    
- Agar tum chahte ho ki **host machine** ya **external clients** us service ko access kar saken, to tumhe **port publish** karna padta hai.
    
- Ye `-p hostPort:containerPort` syntax se hota hai.

example: docker run -dit --name web2 -p 8080:80 my-web
	`-p 8080:80` ka matlab:
- **Host port 8080** → mapped to **container port 80**.


### 3. Multiple published ports

```
docker run -dit --name web3 -p 8080:80 -p 8443:443 my-web
```

👉 Host ke 8080 → container ke 80 👉 Host ke 8443 → container ke 443


## 🔹 Why Published Ports Are Important

- **Expose services** (websites, APIs) to outside world.
    
- **Testing locally** (localhost:8080).
    
- **Production deployment** (EC2 public IP + port).
    
- **Flexibility**: Tum ek hi host pe multiple containers chala sakte ho alag‑alag ports ke saath.