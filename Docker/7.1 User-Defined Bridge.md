# 🔹 User‑Defined Bridge Mode

## 🧠 Definition

- Docker ke default `bridge` ke alawa tum apna **custom bridge network** bana sakte ho.
    
- Isme tumhe **better isolation, DNS resolution (container name se ping), aur control** milta hai.
    
- Containers jo ek hi user‑defined bridge pe hote hain, wo ek‑dusre ko **name se access** kar sakte hain (default bridge me sirf IP se hota hai).

**Fayda:** Agar database restart ho jaye aur uska IP badal bhi jaye, tab bhi backend code change karne ki zaroorat nahi padti kyunki naam (`database`) wahi rehta hai.
## 🔹 Why Use User‑Defined Bridge?

- **Container name resolution** (ping by name, not just IP).
    
- **Isolation**: Containers on `my-net` can’t talk to containers on default `bridge`.
    
- **Flexibility**: You can attach/detach containers to networks.
    
- **Cleaner architecture**: Useful for multi‑container apps (backend + frontend).
## ✅ Summary

- Default bridge → containers talk via IP only.
    
- User‑defined bridge → containers talk via **name** (built‑in DNS).
    
- Command flow:
    
    1. `docker network create my-net`
        
    2. `docker run --network my-net ...`
        
    3. Test with `ping <container-name>`

## 🧪 Practical Example
### 1. Create a custom bridge network

bash

```
docker network create my-net
```

Check:

bash

```
docker network ls
```

👉 Tumhe `my-net` dikhega.

### 2. Run containers on this network

bash

```
docker run -dit --name c1 --network my-net ubuntu:20.04 bash
docker run -dit --name c2 --network my-net ubuntu:20.04 bash
```

### 3. Test communication by name

Inside `c1`:
bash
```
apt update && apt install -y iputils-ping
ping c2
```

👉 Ye **work karega by name** (DNS resolution). Default bridge me ye possible nahi tha.

### 4. Run a webserver on custom network

bash

```
docker run -dit --name webserver --network my-net -p 8080:80 my-web
```

- Container `webserver` ab `my-net` pe hai.
    
- Tum `c1` se `curl http://webserver` kar sakte ho → Apache response milega.
    

## 🔹 Why Use User‑Defined Bridge?

- **Container name resolution** (ping by name, not just IP).
- **Isolation**: Containers on `my-net` can’t talk to containers on default `bridge`.
- **Flexibility**: You can attach/detach containers to networks.
- **Cleaner architecture**: Useful for multi‑container apps (backend + frontend).


## Disconnect command
	docker network disconnect bridge c1
		where - 
			bridge is default network route and c1 is container.

    