ipitil# 1. Access container file to change data

Container me kuch change karna hai - 

	docker exec -it [ContainerName] /bin/bash
	
Now you are under container and perform ubuntu basic operational command like ls.

# 2. Access images to change data

**image template** (Docker image) ko directly edit nahi karte, balki usse naya image banate ho. Let me explain clearly:

## 🔹 Image vs Container
- **Image** = frozen template (read‑only).
- **Container** = running instance of that image (writable layer on top).
👉 You can’t “edit” an image directly. You either:
1. **Edit inside a container** (install packages, change files), then commit those changes into a new image.
2. **Edit the Dockerfile** and rebuild the image (preferred method).
## 🔹 Method 1: Commit Changes from Container

1. Run a container:
    ```
    docker run -it my-web /bin/bash
    ```
2. Make changes (edit files, install software).
3. Save container state as new image:
    ```
    docker commit <container_id> my-web:v2
    ```
👉 This creates a new image with your edits.

## 🔹 Method 2: Edit Dockerfile (Best Practice)

- Update your `Dockerfile` with new instructions:
    
    dockerfile
    
    ```
    FROM my-web:latest
    RUN apt install -y php
    COPY ./newindex.html /var/www/html/index.html
    ```
    
- Rebuild:
    
    bash
    
    ```
    docker build -t my-web:v2 .
    ```
    

👉 This way your changes are reproducible and version‑controlled.



